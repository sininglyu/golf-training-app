## Database
DATABASE_URL="file:./dev.db"

## GHIN integration (unofficial — personal use only)
# 32 random bytes hex-encoded; used to AES-256-GCM encrypt your GHIN password
# at rest. Generate with: `openssl rand -hex 32`
# Rotating this key invalidates all stored GHIN credentials.
GHIN_ENCRYPTION_KEY=""
